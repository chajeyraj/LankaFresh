
Project name: Lanka Drop

1. Install dependencies:
   `npm install`

3. Run the app:
   `npm run dev`

Assets
- Place your logo file at `public/logo.png` (used by Navbar and favicon).
- Place the hero background image at `public/Background.jpg` (referenced by `HomePage.tsx`).
- Keep filenames and capitalization exact (Windows vs case-sensitive deploy targets).
